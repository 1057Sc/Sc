dubbo
DubboConfigConfigurationRegistrar 进行扫描@EnableDubboConfig注解
public DubboBootstrap start() {


-->ExtensionLoader
   	-->DubboBootstrap.start()
   	-->1.exportServices()
   	    -->ServiceConfig.export()
   	        -->doExportUrls()
   	            -->doExportUrlsFor1Protocol(ProtocolConfig, List<URL>)
                    export injvm
                    --> export exportLocal(org.apache.dubbo.common.URL)
   	-->2.exportMetadataService()
   	-->3.registerServiceInstance();
   		-->ProtocolListenerWrapper.export(org.apache.dubbo.rpc.Invoker)
   		    -->ProtocolFilterWrapper.export(org.apache.dubbo.rpc.Invoker)
   		        -->RegistryService.subscribe(overrideSubscribeUrl, overrideSubscribeListener)
   		            -->FailbackRegistry.notify(URL url, NotifyListener listener, List<URL> urls)
   		                -->Configuratort.oConfigurators(List<URL> urls)
